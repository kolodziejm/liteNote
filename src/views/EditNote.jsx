import React from 'react';

// QUERY GET_NOTE. Fetch the note based on the query params id, fill in the snapshot state.

const EditNote = () => {
  return <h1>Hello from EditNote page</h1>;
};

export default EditNote;
